{ pkgs, ... }:
{
  environment.systemPackages = with pkgs; [
    # Edición / desarrollo
    vim
    neovim
    meld
    gcc
    pkg-config
    protobuf
    lldb

    # Kubernetes
    k3s
    kubectl
    k9s
    (pkgs.wrapHelm pkgs.kubernetes-helm {
      plugins = with pkgs.kubernetes-helmPlugins; [
        helm-secrets
        helm-diff
        helm-s3
        helm-git
      ];
    })

    lazygit
    lazydocker

    # Git tooling
    git
    git-absorb
    git-branchless
    delta         # diffs bonitos en terminal
    gnupg         # para firma GPG si la usas

    # Networking / sistema
    curl
    wget
    tailscale
    docker

    # CLI moderna
    fzf
    btop
    httpie
    xh
    bat
    eza
    ripgrep
    fd
    dua         # 'dua-cli' (análisis de disco rápido)
    nitch    # info del sistema (alternativa a 'nitch')

    # SSL/CA
    openssl
    cacert
  ];
}
